"use client";

import { memo } from "react";

/**
 * Unused ProfileMenu component placeholder for compilation purity
 */
function ProfileMenuContent() {
  return null;
}

export const ProfileMenu = memo(ProfileMenuContent);