# Execution Rule

Once a feature has an approved vision and architecture, stop generating implementation plans.

Proceed directly to implementation.

Only pause when:

- a major architectural conflict appears,
- new assets are required,
- a design decision changes the user experience significantly.

Otherwise:

Build → Review → Refine → Commit.